---
# - name: 'update the packages.'
#   become: yes
#   apt:
#     upgrade: yes
#     update_cache: yes
#     cache_valid_time: 86400

# - name: delete  deps
#   become: yes
#   apt:
#     autoremove: yes

# - name: create pm2
#   become: yes
#   npm:
#     name: pm2
#     global: yes

- name: extract all artifact resources
  become: yes
  unarchive:
    src: files/artifact.tar.gz
    dest: .
    
- name: start the application 
  become: yes
  shell: |
    npm install
    pm2 stop all
    pm2 start npm --name "udapeople-backend" -- run start
  environment:
    ENVIRONMENT: production
    NODE_ENV: production
    TYPEORM_CONNECTION: postgres
    TYPEORM_MIGRATIONS_DIR: ./src/migrations
    TYPEORM_ENTITIES: ./src/modules/**/*.entity.ts
    TYPEORM_MIGRATIONS: ./src/migrations/*.ts
    TYPEORM_HOST: mydbinstance.clmy6deziydj.us-east-1.rds.amazonaws.com
    TYPEORM_PORT: 5432
    TYPEORM_USERNAME: postgres
    TYPEORM_PASSWORD: udapeople123AICw3Ab0PaVTFtXFSF8b
    TYPEORM_DATABASE: postgres
    
- name: "Configure and then start the pm2"
  become: yes
  shell: |
    env PATH=$PATH:/usr/local/bin pm2 startup -u ubuntu
- name: verify the status of pm2
  become: yes
  shell: |
    pm2 describe "udapeople-backend"
  register: command_output_npm
- debug: var=command_output_npm.stdout_lines